json.extract! department, :id, :name, :manager, :via, :civico, :cap, :citta, :provincia, :description, :floors, :spaces, :slot, :apertura, :chiusura, :created_at, :updated_at
json.url department_url(department, format: :json)
