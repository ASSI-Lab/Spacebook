<%= form_with(model: favourite_space) do |form| %>
  <% if favourite_space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favourite_space.errors.count, "error") %> prohibited this favourite_space from being saved:</h2>

      <ul>
        <% favourite_space.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :department %>
    <%= form.text_field :department %>
  </div>

  <div class="field">
    <%= form.label :typology %>
    <%= form.text_field :typology %>
  </div>

  <div class="field">
    <%= form.label :space %>
    <%= form.text_field :space %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
