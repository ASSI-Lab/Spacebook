<!DOCTYPE html>
<html lang="it">
<head>
	<meta charset="UTF-8">
   	<meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
	<title> Spazi Preferiti - Spacebook </title>
	<%= csrf_meta_tags %>
	<%= csp_meta_tag %>
	<%= stylesheet_link_tag 'favourite_spaces', media: 'all', 'data-turbolinks-track': 'reload' %>
	<%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

	<script>
		$(document).ready(function() {
			$('#datatablesDipartimenti').DataTable();
			$('#datatablesUtenti').DataTable();
			$('#datatablesPrenotazioni').DataTable();
		} );
	</script>

</head>

<body>

<p id="notice"><%= notice %></p>



<div class="sb-nav-fixed">
	<div id="layoutSidenav_content">
		<main>
			<div class="container-fluid px-4">
				<div class="card mb-4">
					<div class="card-header">
						<i class="fas fa-table me-1"></i>
						Favourite Spaces
					</div>
					<div class="card-body">
						<table id="datatablesDipartimenti" class="table table-responsive table-striped table-bordered table-hover">
							<thead>
	
                <tr>
                  <th>Email</th>
                  <th>Department</th>
                  <th>Typology</th>
                  <th>Space</th>
                  <th colspan="3"></th>
                </tr>
              </thead>

              <tbody>
                <% @favourite_spaces.each do |favourite_space| %>
                  <tr>
                    <td><%= favourite_space.email %></td>
                    <td><%= favourite_space.department %></td>
                    <td><%= favourite_space.typology %></td>
                    <td><%= favourite_space.space %></td>
                    <td><%= link_to 'Show', favourite_space %></td>
                    <td><%= link_to 'Edit', edit_favourite_space_path(favourite_space) %></td>
                    <td><%= link_to 'Destroy', favourite_space, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
		</main>
	</div>
</div>

<!-- <%= button_to 'New Favourite Space', new_favourite_space_path %> -->


</body>
</html>